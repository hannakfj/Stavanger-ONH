import { Box } from "@mui/system";
import React from "react";

type InfoBoxProps = {
  title: string;
  image: string;
};

const InfoBox: React.FC<InfoBoxProps> = ({ title, image }) => {
  return <Box></Box>;
};
export default InfoBox;
